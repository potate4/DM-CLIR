# Core dependencies
python>=3.8

# Web crawling
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
feedparser>=6.0.10

# ====== MODULE B: Query Processing ======

# Translation
deep-translator>=1.11.0          # Google Translate API (primary)
transformers>=4.35.0             # MarianMT models (fallback)
torch>=2.0.0                     # Required for transformers
sentencepiece>=0.1.99            # Required for MarianMT tokenizer

# Query Expansion
nltk>=3.8.0                      # WordNet, stemming, lemmatization
gensim>=4.3.0                    # Word embeddings (GloVe, Word2Vec)

# Named Entity Recognition
spacy>=3.7.0                     # NER extraction

# ====== MODULE A & C: Indexing & Retrieval ======

# Embeddings
sentence-transformers>=2.2.0     # Sentence embeddings for semantic search

# Indexing and search
whoosh>=2.7.4
rank-bm25>=0.2.2

# Fuzzy matching
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.23.0

# Data handling
pandas>=2.1.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Configuration
pyyaml>=6.0.1

# Progress bars and logging
tqdm>=4.66.0

# Visualization
matplotlib>=3.8.0

# Testing
pytest>=7.4.0

# ====== Post-install commands ======
# Run these after pip install:
#
# Download spaCy English model:
#   python -m spacy download en_core_web_sm
#
# Download NLTK data:
#   python -c "import nltk; nltk.download('wordnet'); nltk.download('omw-1.4')"
#
# MarianMT models download automatically on first use
